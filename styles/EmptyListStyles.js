import { StyleSheet } from "react-native";

export const noTeamStyles = StyleSheet.create({
    noTeamsImg: {
        width: '100%',
        height: 450
    }
})

export const noPlayerStyles = StyleSheet.create({
    noTeamsImg: {
        width: '100%',
        height: 450
    }
})